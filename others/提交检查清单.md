# 项目提交检查清单

## ✅ 已完成的文件

### 1. README.txt ✅
- [x] 已创建 `README.txt` 文件
- [x] 包含安装和运行说明
- [x] 格式为纯文本文件

### 2. sources.zip ✅
- [x] 脚本会自动创建包含所有Java源文件的ZIP
- [x] 保持文件夹结构（src/main/java/...）

### 3. 可执行jar文件 ✅
- [x] pom.xml已配置maven-shade-plugin
- [x] 会生成包含所有依赖的fat jar
- [x] 主类设置为 `com.bigcomp.accesscontrol.Main`

### 4. files.zip ✅
- [x] 脚本会自动创建包含data和images目录的ZIP
- [x] 保持文件夹结构

### 5. db.sql ✅
- [x] 已创建 `DatabaseExporter` 工具类
- [x] 可以将SQLite数据库导出为MySQL格式
- [x] 包含表结构和数据

## ⚠️ 需要您手动完成的文件

### 6. project-report.pdf ⚠️
- [ ] 使用提供的 `project-report.docx` 模板
- [ ] 填写完整的项目报告
- [ ] 包含Youku视频片段的网址
- [ ] 转换为PDF格式

### 7. 视频 ⚠️
- [ ] 录制15-18分钟的视频
- [ ] 团队每位成员必须发言（每人5-6分钟）
- [ ] 介绍团队并演示程序
- [ ] 上传到Youku
- [ ] 在报告中提供视频链接

## 📋 提交步骤

### 步骤1：运行准备脚本

**Windows系统：**
```bash
prepare_submission.bat
```

**Linux/Mac系统：**
```bash
chmod +x prepare_submission.sh
./prepare_submission.sh
```

### 步骤2：检查生成的文件

脚本会在 `submission_temp` 目录中生成以下文件：

1. **README.txt** - 安装和运行说明
2. **sources.zip** - 所有Java源文件
3. **access-control-system-1.0.0.jar** - 可执行jar文件
4. **files.zip** - data和images目录
5. **db.sql** - MySQL格式的数据库文件

### 步骤3：准备报告和视频

1. 下载 `project-report.docx` 模板
2. 填写项目报告
3. 录制演示视频并上传到Youku
4. 在报告中添加视频链接
5. 将报告转换为PDF格式

### 步骤4：最终检查

在提交前，请确认：

- [ ] README.txt 文件存在且格式正确
- [ ] sources.zip 包含所有Java源文件（保持文件夹结构）
- [ ] access-control-system-1.0.0.jar 可以正常运行
- [ ] files.zip 包含data和images目录
- [ ] db.sql 格式正确，可以导入MySQL
- [ ] project-report.pdf 已填写完整
- [ ] 视频已上传到Youku，链接已添加到报告中

### 步骤5：提交

将所有6个文件上传到eLearning平台：
1. README.txt
2. sources.zip
3. access-control-system-1.0.0.jar
4. files.zip
5. db.sql
6. project-report.pdf

## 🔍 验证清单

### 验证jar文件
```bash
java -jar access-control-system-1.0.0.jar
```
应该能正常启动GUI界面。

### 验证sources.zip
解压sources.zip，确认包含：
- src/main/java/com/bigcomp/accesscontrol/... 所有Java文件

### 验证files.zip
解压files.zip，确认包含：
- data/ 目录（包含profiles, groups, logs等）
- images/ 目录（包含site-layout.png, office-layout.png）

### 验证db.sql
使用MySQL客户端或命令行测试：
```sql
mysql -u root -p < db.sql
```
应该能成功导入数据库。

## 📝 注意事项

1. **团队成员信息**：确保所有Java源文件顶部都有团队成员名字的注释
2. **文件格式**：README必须是.txt格式，报告必须是.pdf格式
3. **文件夹结构**：ZIP文件必须保持原有的文件夹结构
4. **数据库格式**：db.sql必须是MySQL兼容格式
5. **视频要求**：15-18分钟，每位成员发言5-6分钟

## ❓ 常见问题

**Q: jar文件无法运行？**
A: 确保使用maven-shade-plugin生成的fat jar，包含所有依赖。

**Q: db.sql导入失败？**
A: 检查SQL语法，确保使用MySQL兼容的语法。

**Q: sources.zip缺少文件？**
A: 确保脚本正确扫描了src目录下的所有Java文件。

**Q: files.zip太大？**
A: 可以排除logs目录中的历史日志文件，只保留必要的配置文件。

